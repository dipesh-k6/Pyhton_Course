# The four pillars of OOP:
    Encapsulation — bundle data + functions together

    Inheritance — child class gets parent's features

    Abstraction — hide complex details, show simple interface

    Polymorphism — same method name, different behavior

---------------------------------------------------------------------------------

# Useful dunder methods:
    Dunder              Triggered by(example)
    __init__            Rectangle()
    __str__             print(rectangle)
    __add__             rectangle1 + rectangle2
    __len__             len(rectangle)
    __eq__              rectangle1 == rectangle2
    __gt__              rectangle1 > rectangle2